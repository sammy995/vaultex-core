# Optional — copy to .env and fill in your keys
# These are only needed if you route to cloud LLMs.
# For Ollama (fully local), no keys are required.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
